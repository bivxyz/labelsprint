# LabelSprint does not require API keys for the browser app.
# Keep secrets out of frontend code and configure any future server-only values
# in the hosting/runtime environment, not in files committed with the app.

# Public HTTPS URL used by the optional Google Workspace sidebar.
VITE_PUBLIC_APP_URL="https://labelsprint.app"
